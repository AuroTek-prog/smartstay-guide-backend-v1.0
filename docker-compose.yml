services:
  api:
    build: .
    env_file:
      - /etc/smartstay-backend.env
    ports:
      - "3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - /etc/smartstay/firebase-service-account.json:/etc/smartstay/firebase-service-account.json:ro
